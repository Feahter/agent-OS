# Agent OS 长期路线图

[English](ROADMAP.md) | [简体中文](ROADMAP.zh-CN.md)

这份路线图描述 Agent OS 从预览版走向通用 Agent 操作层的长期方向。版本号表示能力成熟度，不承诺固定日期；每个阶段只有达到用户结果和质量门槛后才会发布。

## 北极星

**用户只需说明目标和约束，Agent OS 负责选择、编排和治理合适的 Agent，持续交付可验证、可恢复、成本可控的结果。**

对普通用户，图、模型路由、上下文整理、并发、重试和状态迁移都应成为系统内部实现。用户主要面对四件事：目标、关键选择、进度和结果。

理想的日常入口最终应接近：

```bash
agent-os do "修复登录超时，补测试，验证后提交"
```

系统自动完成环境检查、任务规划、执行器选择、隔离执行、验证、失败恢复和经验沉淀；只在权限、费用或不可逆操作等关键节点请求确认。

## 产品原则

1. **从用户目标开始。** 用户描述想得到的结果，不需要先学习 GraphSpec 或各家 Agent 的参数。
2. **默认简单，按需展开。** 常用任务提供可靠默认值；图、路由、预算和策略仍可逐层查看和覆盖。
3. **一次说明，多 Agent 协作。** Codex、Claude Code、Pi、Orca 及后续工具通过统一任务接口接入，切换工具不改变用户工作方式。
4. **结果必须可相信。** 完成不是 Agent 自述，而是由测试、审查、Reality Anchor 和可追溯证据共同确认。
5. **失败可以恢复。** 任务可暂停、续跑、回滚和迁移；进程崩溃不应让用户重新开始或重复副作用。
6. **越用越顺手。** 系统学习用户偏好、项目惯例和成功路径，减少重复说明与不必要确认。
7. **越用越高效。** 在质量门槛不下降的前提下，持续降低等待时间、模型调用量和费用。
8. **学习受治理。** RSI 只能在明确的安全、质量和预算范围内提出、评估、灰度和回滚改进。
9. **本地优先，随时可迁移。** 用户掌握数据、凭据和运行状态；核心状态采用开放、版本化格式，不锁定模型、Agent 或云平台。
10. **一个控制面。** 状态、审批、费用、证据和策略集中呈现，不让用户在多个工具之间寻找真相。

## 用户体验契约

Agent OS 的公共界面应逐步收束为五个稳定动作：

| 用户动作 | 用户得到什么 |
| --- | --- |
| `do` | 用自然语言提交目标，可附带时间、质量和费用约束。 |
| `status` | 看懂任务正在做什么、为何等待、已花多少、预计还需多少。 |
| `approve` | 只处理真正需要人的关键决定，并看到风险与影响。 |
| `control` | 暂停、继续、取消、调整优先级或切换策略。 |
| `result` | 获得产物、验证证据、改动摘要、费用和可复用经验。 |

底层模块应围绕这些动作形成深模块：公共接口保持小而稳定，复杂的图编译、Agent 差异、上下文管理、恢复和学习留在实现内部。高级用户仍可直接使用 GraphSpec 和策略接口，但它们不应成为入门前提。

## 当前交付快照

截至 2026-08-24，仓库对外版本仍为 `0.0.1` 预览版，但实现已经覆盖部分 0.1 和 0.2 能力。下方版本号表示成熟度和用户证据，不表示功能是否已有第一版实现。

| 工作包 | 状态 | 已有证据与剩余门槛 |
| --- | --- | --- |
| 可信底座 | 预览版已发布 | 图执行、Artifact、审批、预算、恢复、受控合并、安全复用、受治理学习和可迁移状态已经实现。 |
| P1：真实项目评测 | P1.1–P1.3 完成；P1.4 待执行 | 已有隐私最小化记录和不可覆盖基线；仍需三个经授权的真实项目灰度，建立质量、人工介入、耗时和费用基线。 |
| P2：统一任务接口 | 预览版完成 | `do / status / approve / control / result` 共用一份任务状态，GraphSpec 保留为高级接口。 |
| P3：意图编译 | 预览版完成 | 修复、测试、重构、调研和发布准备可编译为可审阅计划，并使用安全默认值。 |
| P4：本地常驻协调 | 预览版完成 | 后台队列、优先级、暂停/继续/取消、崩溃恢复、任务摘要、通知与 Orca 生命周期已收束到一个控制面。 |
| P5：兼容与故障 | P5.1–P5.2 完成；P5.3 待执行 | 已有版本化、平台限定的协议证据、运行时认证硬闸门、离线故障注入和带可执行修复建议的零模型首次设置；当前认证范围为 Darwin arm64，全新环境与真实模型输出/Orca 生命周期灰度仍待验证。 |
| P6：个人化 RSI 与成本 | 待开始 | 在真实项目基线完成后启动；记忆和策略提升必须可查看、可撤销，并受冻结质量门槛约束。 |

合并执行的 P1.4/P5.3 是近期唯一会主动消耗模型 token 或修改外部项目的工作包。它必须保持选择加入，并明确项目、预算、停止条件和人工介入记录。

## 分阶段路线

### 0.0.x：可信底座（当前发布线）

目标：证明多个 Agent 可以在同一套受治理的执行语义下工作。

已有基础包括图运行时、Artifact 契约、Codex/Claude Code/Pi/OpenCode Adapter、Orca 协调、审批、预算、恢复、受控合并、安全复用、Reality Anchor、受治理 RSI 和状态迁移。仓库也包含后续里程碑的预览实现；在用户结果门槛通过前，这些能力不视为成熟。

阶段退出条件：

- **已达到：**核心契约拥有稳定的自动化测试和故障注入覆盖。
- **待授权：**至少完成三个真实项目的小流量试用，记录成功、失败、人工介入和费用基线。
- **进行中：**明确区分预览接口与后续需要保持兼容的公共接口。

### 0.1：第一次使用就能成功

目标：用户不写图、不配复杂策略，也能在几分钟内完成第一个真实任务。

主要交付：

- 一条命令安装、环境自检和支持的 Agent 自动发现。
- 发现更多本地 Agent，同时把“已安装”与“已认证可执行”严格分开。
- `agent-os do` 自然语言任务入口，以及目标、约束到执行图的意图编译器。
- 修复、测试、重构、调研和发布准备等高频任务模板。
- 自动选择安全默认值；只有缺少关键上下文时才提问。
- 面向人的进度、错误和结果摘要，隐藏内部 JSON 噪声。
- 真实 Codex、Claude Code、Pi、OpenCode 和 Orca 的端到端兼容测试。

预览状态：统一任务动作、意图编译器、首批五类模板、常驻执行和面向人的 `agent-os setup` 首次使用流程已经实现。Setup 会安全初始化本地状态、判断就绪级别并给出结构化修复建议，全程不调用模型；OpenCode 已在 Darwin arm64 上晋升为认证 Adapter，OpenClaw、Hermes Agent、Aider、Gemini CLI 和 GitHub Copilot CLI 仍保持仅发现。剩余门槛是全新环境演练、下一项高优先级 Adapter 认证，以及经授权的真实项目灰度。

阶段退出条件：新用户在无 GraphSpec 知识的情况下，五分钟内启动首个任务；常见单仓库任务可从目标走到验证结果；中断后可一条命令续跑。

### 0.2：成为日常工作入口

目标：用户愿意把持续数分钟到数小时的日常 Agent 工作交给 Agent OS。

主要交付：

- 长期驻留的本地协调器，支持后台执行、排队、优先级和计划任务。
- 统一任务中心：进度、依赖、审批、日志摘要、费用、证据和产物血缘。
- 暂停、继续、取消、重试和从检查点恢复。
- 通知与审批收件箱，避免用户盯着终端等待。
- 项目配置自动发现，支持可继承的个人、团队和项目策略。
- 稳定的 Agent Adapter 开发包及协议兼容测试。

预览状态：常驻队列、统一生命周期、任务中心、通知和恢复路径已经实现。公共 `CliAgentAdapter` 开发包现已支撑四个认证执行器，没有增加第二套运行时接缝。剩余工作是长期运行证据、可发现配置，以及在冻结兼容承诺前取得外部一致性使用证据。

阶段退出条件：长任务在退出终端后继续运行；常见故障可自助恢复；用户不需要进入各 Agent 工具查看任务真相。

### 0.3：个人化 RSI 与成本智能

目标：在质量不下降的前提下，让同类任务越做越少打扰、越快、越便宜。

主要交付：

- 用户偏好、项目惯例和成功工作流的分层记忆，支持查看、纠正、遗忘和导出。
- 根据任务类型动态选择 Agent、模型、上下文、并发度和验证强度。
- 语义级结果复用、增量上下文和重复任务合并。
- 质量、延迟、费用和人工介入的联合优化，不只追求单一最低价格。
- 每次策略变化都可解释：学到了什么、为何改变、影响哪些任务。
- 冻结回归集、离线评估、人工批准、灰度和自动回滚组成完整 RSI 闭环。

预览状态：无 prompt 观测、受治理候选、回归评估、审批、灰度和回滚基础已经实现。个人与项目记忆控制、真实任务基线和可测量的成本改进仍待完成。

阶段退出条件：在固定回归任务集上质量不下降；重复任务的费用和完成时间出现可测量下降；用户可以清楚撤销任何学习结果。

### 0.5：团队 Agent OS

目标：让团队共享可靠工作方式，同时保持权限、数据和责任边界清晰。

主要交付：

- 多项目、跨主机任务调度和可靠的分布式状态。
- 工作区、团队和租户隔离，细粒度角色、权限和预算配额。
- 团队策略包、验证模板、共享 Artifact 与经过验证的经验库。
- GitHub/GitLab、CI、Issue、消息和知识库的标准连接器。
- 审计、数据保留、脱敏、密钥生命周期和合规导出。
- 团队级容量、质量、费用与失败模式视图。

阶段退出条件：两个以上团队可在同一部署中安全隔离；任务可跨主机恢复；每个外部副作用都能追溯到目标、审批和执行证据。

### 0.8：开放生态与可组合能力

目标：新 Agent、新工具和新工作流无需修改核心运行时即可安全接入。

主要交付：

- 稳定的 Adapter、工具、Artifact、策略和工作流扩展接口。
- 带权限声明、兼容性检查、签名和来源证明的扩展清单。
- 可发现、可测试、可版本化的工作流与策略包。
- Agent 能力协商和协议一致性测试套件。
- 沙箱、最小权限和供应链验证。

阶段退出条件：第三方可仅依赖公开接口完成扩展；扩展升级失败不会破坏核心状态；用户安装前能看懂权限、费用和数据影响。

### 1.0：稳定的 Agent 操作层

目标：提供可长期依赖、可升级、可迁移的 Agent OS 核心契约。

主要交付：

- 稳定的任务、Artifact、Adapter、事件、策略和状态格式。
- 有期限承诺的兼容政策、自动迁移、备份恢复和降级路径。
- macOS、Linux 的正式支持；根据真实需求评估 Windows。
- 安全模型、威胁分析、发行签名、软件物料清单和可复现构建。
- 性能与可靠性目标、容量模型和长期运行测试。
- 从个人本地模式到团队部署的一致操作体验。

阶段退出条件：升级和迁移经过真实历史状态验证；核心用户旅程达到公开可靠性目标；普通用户无需理解内部图结构即可稳定完成工作。

### 1.x 及以后：Agent 计算平台

在 1.0 契约稳定之后，再探索跨设备协作、隐私保护的群体学习、组织级资源市场和更广泛的知识工作场景。任何扩展都必须继续服从简单、可信、高效和用户可控四项原则。

## 横向建设主线

每个版本都应同时推进以下主线，避免先堆功能、最后补体验和治理：

| 主线 | 长期结果 |
| --- | --- |
| 交互 | 从命令和配置转向目标驱动、渐进展开的一致体验。 |
| 编排 | 从手写图转向可解释的意图编译和动态执行计划。 |
| 可信 | 从任务级验证扩展到证据链、供应链和长期审计。 |
| 智能 | 从固定路由演进到受治理、可纠正、可回滚的 RSI。 |
| 效率 | 联合优化时间、调用量、缓存命中和人工介入。 |
| 生态 | 用稳定 seam 接入 Agent、工具、工作流和外部系统。 |
| 可移植 | 状态、策略、记忆和 Artifact 都可导出、迁移和升级。 |

## 衡量是否真的对用户更好

路线图不以模块数量衡量进展，而以用户结果衡量：

| 维度 | 核心指标 |
| --- | --- |
| 方便 | 首次成功时间、完成任务所需输入次数、安装成功率。 |
| 简单 | 每个任务的人工决策数、用户必须理解的概念数、错误自助解决率。 |
| 高效 | 端到端完成时间、排队时间、有效并发率、重复工作消除率。 |
| 可信 | 首次验证通过率、误报完成率、恢复成功率、不可追溯副作用数。 |
| 经济 | 每个验证成功结果的费用，而不是每次模型调用的价格。 |
| 智能 | 同类任务的重复说明减少率、策略提升率、回归与回滚率。 |
| 可控 | 审批等待时间、用户纠正生效率、记忆删除与状态迁移成功率。 |

所有 RSI 优化必须满足硬约束：安全不降级、质量不低于基线、费用和延迟变化可见、用户可以退出和回滚。遥测默认本地保存；任何外发分析都应明确选择加入并可检查内容。

## 近期优先级

下一轮实现按以下顺序展开：

1. **明确授权后合并执行 P1.4/P5.3 灰度。**选择三个低风险项目，使用固定预算，记录 Codex、Claude Code、Pi 和 Orca 的验证成功、误报完成、人工介入、耗时、token 和费用。
2. **补齐全新环境证据。**零模型 `setup` 流程已经可以初始化状态、区分支持/未认证/故障状态并给出可执行修复建议；下一步在全新环境演练，并为各支持版本与平台补齐证据。
3. **定义 0.1 兼容线。**明确任务、Artifact、事件、策略和状态接口中哪些属于公共契约；承诺稳定前补齐迁移 fixture 和升级演练。
4. **基于冻结基线启动 P6 个人化 RSI。**先提供记忆的查看、纠正、遗忘和导出，再在质量与预算门槛内优化 Agent、模型、上下文和验证选择。
5. **加固 Agent Adapter 开发包并选择下一项接入。**先用内置执行器之外的实现检验公共 Kit，冻结最小兼容面；再按用户价值、非交互协议稳定性、权限控制和维护成本评估 OpenClaw 与 Hermes，择一实现。

## 明确不做

- 不替代 Codex、Claude Code、Pi 或其他专业 Agent；Agent OS 负责组织和治理它们。
- 不要求普通用户手写任务图。
- 不用更多 Agent、更多并发或更大模型冒充更智能。
- 不允许 RSI 绕过权限、预算、验证或人工审批。
- 不把云服务设为使用核心能力的前提。
- 不用不可导出的记忆、私有格式或隐藏策略锁定用户。

## 路线图治理

每个里程碑进入开发前，应写明用户问题、目标指标、非目标、风险、迁移影响和退出条件。进入发布前，应有真实任务证据，而不只依赖单元测试。路线图更新必须区分已有实现、已发布契约、待补证据和需要用户授权的工作。路线图可随反馈调整，但北极星、用户体验契约、安全底线和可迁移原则只能通过明确的设计决策变更。
